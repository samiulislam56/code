#include<stdio.h>
int main()
{
    int n,m;
    printf("Enter two number:");
    scanf("%d%d", &n,&m);

    int gcd=1;
    for(int i=1; i<=m && i<=n; i++){
        if(m%i==0 && n%i==0){
            gcd = i;
        }
    }
    printf("gcd of %d and %d is %d", m,n,gcd);

    return 0;
}
